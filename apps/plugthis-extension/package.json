{
  "name": "@the-leverage-lab/plugthis-extension",
  "version": "0.1.0",
  "private": true,
  "description": "PlugThis.ai / The Leverage Lab Chrome MV3 extension (mock-first Worker client)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=18"
  }
}
